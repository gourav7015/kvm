[package]
name = "kvm-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
kvm-identity = { version = "0.1.0", path = "../identity" }
kvm-input = { version = "0.1.0", path = "../input" }
kvm-net = { version = "0.1.0", path = "../net" }
kvm-protocol = { version = "0.1.0", path = "../protocol" }
thiserror = "2.0.20"
tokio = { version = "1.53.1", features = ["rt-multi-thread", "macros", "net", "time", "sync", "io-util"] }
tracing = "0.1.41"

[dev-dependencies]
quinn = "0.11.11"
tempfile = "3.27.0"
